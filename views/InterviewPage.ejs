<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="../styles/normalize.css" rel="stylesheet" type="text/css">
    <link href="../styles/GlobalStyles.css" rel="stylesheet" type="text/css">
    <link href="../styles/InterviewPageStyles.css" rel="stylesheet" type="text/css">
    <script src="../scripts/InterviewPageScript.js" defer></script>
    <title>Interview Page</title>
</head>
<body class="interview-page">
    <header>
        <div class="left">
            <a href="../pages/indexPage.html" class="nav-link">STRONA GŁÓWNA</a>
            <a href="/offerPage" class="nav-link">OFERTA</a>
            <a href="../pages/AboutUsPage.html" class="nav-link">O NAS</a>
        </div>
        <div class="middle">
            <a href="../pages/indexPage.html" class="nav-link">
                <img src="../images/logo.png" alt="Logo" id="logo">
            </a>
        </div>
        <div class="right">
            <a href="../pages/ContactPage.html" class="nav-link">KONTAKT</a>
            <button class="clients-portal" id="clients-portal">PANEL KLIENTA</button>
        </div>
    </header>
    <div class="interview-container">
        <form id="interview-form">
            <p class="interview-title">Wywiad z klientem</p>
            <% questions.forEach((question) => { %>
                <div class="question 
                    <% if (question.category === 'TF') { %>
                        tf-category
                    <% } else if (question.category === 'TF_EXPLANATION') { %>
                        tf-explanation-category
                    <% } else if (question.category === 'DESCRIPTIVE') { %>
                        descriptive-category
                    <% } else { %>
                        default-category
                    <% } %>">
                        <label class="question-label"><%= question.content %></label>
            
                        <div class="radio-container">
                           <% if (question.category === 'TF' || question.category === 'TF_EXPLANATION') { %>
                                <input type="radio" class="radio-option" name="question_<%= question.question_id %>" value="true"> Tak
                                <input type="radio" class="radio-option" name="question_<%= question.question_id %>" value="false"> Nie
                            <% } %>
                        </div>
    
                        <% if (question.category === 'TF_EXPLANATION' || question.category === 'DESCRIPTIVE') { %>
                            <textarea name="explanation_<%= question.question_id %>" placeholder="Jakie? Proszę sprecyzować" required></textarea>
                        <% } else { %>
                         <!-- Empty div to maintain grid structure -->
                            <div></div>
                        <% } %>
                </div>
            <% }); %>
            <div>
                <p class="info-for-client">Przed naciśnięciem przysiku "Wyślij" proszę przekazać urządzenie pracownikowi w celu weryfikacji poprawności wywiadu</p>
            </div>
            <div class="submit-container">
                <button type="submit" class="submit-button">Wyślij</button> 
            </div>
        </form>
    </div>  
</body>
</html>